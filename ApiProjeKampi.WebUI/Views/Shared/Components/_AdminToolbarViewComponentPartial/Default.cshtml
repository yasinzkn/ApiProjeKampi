<div class="glass-toolbar">
    <input class="search-input"
           placeholder="@ViewBag.Placeholder" />

    @if (ViewBag.ShowCategory)
    {
        <select class="search-input" style="max-width:180px;">
            <option>Tüm Kategoriler</option>
        </select>
    }

    <button class="btn-glass-create"
            style="background:linear-gradient(135deg,#ffd36b,#ff9ec4);color:#111">
        <i class="fas fa-undo"></i> Sıfırla
    </button>
</div>
